<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/imageSection"
    android:layout_margin="5dp"
    card_view:cardCornerRadius="5dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="5dp"
        android:layout_margin="5dp">

        <ImageView
            android:id="@+id/yearPhoto"
            style="@style/ic_image_photo"
            android:contentDescription="@string/image_photo" />
        <TextView
            android:id="@+id/description"
            style="@style/ic_description"
            android:layout_toEndOf="@id/yearPhoto"/>
        <TextView
            android:id="@+id/footerNote"
            style="@style/ic_footer"/>

    </RelativeLayout>

</android.support.v7.widget.CardView>
