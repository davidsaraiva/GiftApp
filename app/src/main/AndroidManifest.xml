<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.ds.giftApp">

    <application
        android:name="pt.ds.giftApp.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="pt.ds.giftApp.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="pt.ds.giftApp.AboutActivity"
            android:parentActivityName="pt.ds.giftApp.MainActivity"/>
        <activity android:name="pt.ds.giftApp.PresentationActivity"
            android:parentActivityName="pt.ds.giftApp.MainActivity"/>
    </application>

</manifest>